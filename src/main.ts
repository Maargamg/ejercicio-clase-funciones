function sumar () : void{
    const sumar = document.getElementById("sumar");
    const resultado = parseInt("sumar") , ++1;
    buttonSumar.addEventListener("click", sumar);
}

function restar () : void{
    const restar = document.getElementById("restar");
    const resultado = parseInt("restar") , --1;
    buttonRestar.addEventListener("click", restar);
}


function reset (): void{
    const reset = document.getElementById("reset");
    const resultado = parseInt("reset") ==0;
    buttonReset.addEventListener("click", reset);
}

function manual (): void{
    const marcador = document.getElementById("marcador");
    const resultado = parseInt("manual");
    IdMarcador.addEventListener("click", );

}


padStart

